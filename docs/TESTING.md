# Testing

```
npm test          # perft, SAN, PGN, 960, ECO, WAC mate, divide, NNUE
npm run perft     # startpos
npm run divide 3  # root split
npm run bench
npm run selfplay 2
npm run match 2
```

Perft suite: start, Kiwipete, pos3–6 (CPW).
Tactics: `src/data/wac.js`.
